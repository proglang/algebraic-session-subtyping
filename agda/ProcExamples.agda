module ProcExamples where

-- The examples in this legacy module all used the removed `Process` syntax
-- and its syntax-directed transition relation.  Operational examples for the
-- maintained flat configuration semantics live in `ProcExamplesFresh`.
